<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.turingcourt.dao.CommentLikesDao">

    <resultMap id="BaseResultMap" type="com.turingcourt.entity.CommentLikes">
        <!--@Table comment-likes-->
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="cid" column="cid" jdbcType="INTEGER"/>
        <result property="uid" column="uid" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="BaseResultMap">
        select
          id, cid, uid
        from turingblog.comment-likes
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="BaseResultMap">
        select
          id, cid, uid
        from turingblog.comment-likes
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="BaseResultMap">
        select
          id, cid, uid
        from turingblog.comment-likes
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="cid != null">
                and cid = #{cid}
            </if>
            <if test="uid != null">
                and uid = #{uid}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into turingblog.comment-likes(cid, uid)
        values (#{cid}, #{uid})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update turingblog.comment-likes
        <set>
            <if test="cid != null">
                cid = #{cid},
            </if>
            <if test="uid != null">
                uid = #{uid},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from turingblog.comment-likes where id = #{id}
    </delete>

</mapper>